
const CallToJoinScreen = ({onClick, meetingId}) => {
  return (
    <button onClick={onClick} className="w-full h-full bg-black flex items-center justify-center flex-col">Join</button>
  )
}

export default CallToJoinScreen